services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        FONT_AWESOME_TOKEN: ${FONT_AWESOME_TOKEN}
    env_file:
      - .env.local
    ports:
      - "3004:3004"
